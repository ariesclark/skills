id: skill-when-to-use-length
language: markdown
severity: error
message: >-
  Skill `when_to_use` exceeds 512 characters. Combined with `description` it can
  be truncated in the skill listing, silently dropping the trailing trigger
  phrases.
files:
  - "**/SKILL.md"
rule:
  kind: minus_metadata
  regex: "when_to_use:(?:[^\\n]|\\n[ \\t]){513,}"
